# Supply-chain hardening: never run install scripts from transitive packages,
# always resolve against the lockfile, and fail on integrity mismatch.
ignore-scripts=true
audit-level=moderate
fund=false
